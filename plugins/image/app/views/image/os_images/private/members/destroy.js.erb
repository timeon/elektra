<% if @member.errors.size.positive? %>
  $('#members .form-wrapper .error').text('<%=@member.errors.full_messages.to_sentence%>');
  $("#member_<%=params[:id]%>").removeClass('updating');
<% else %>
  $('#members .form-wrapper .error').text('');
  $("#member_<%=params[:id]%>").remove();
<% end %>
